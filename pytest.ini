[pytest]
asyncio_mode = auto
markers =
    unit: Unit tests
    integration: Integration tests
    twilio: Tests that interact with Twilio API
    asyncio: Mark test as async/await test 
addopts = -v --tb=short 